name: secret test

on:
  workflow_call:
    secrets:
      TEST_POP:
        required: true

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: jksdhfjkahd
        env:
          TEST_POP: ${{ secrets.POP }}
        run: |
          echo -n $TEST
          echo -n $TEST_POP | wc -m
